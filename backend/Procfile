web: python -c "import os,sys; p='backend' if os.path.isdir('backend') else '.'; sys.path.insert(0,p); import uvicorn, asgi_app; uvicorn.run(asgi_app.app, host='0.0.0.0', port=int(os.environ.get('PORT','8000')), proxy_headers=True, forwarded_allow_ips='*', log_level='info')"
